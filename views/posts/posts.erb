<div class = "container small">
  <% @posts.each do |post| %>
  <h2><%= post.title %></h2>
  <p><%= post.content %></p>
  <p><%= post.created_at %></p>
  <p>By <a href="/posts/<%= post.user_id %>"><%= User.find(post.user_id).username %></a></p>
  <% end %>
</div>
